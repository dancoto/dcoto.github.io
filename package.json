{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://dcoto.github.io/",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "git checkout master; cp -r build/* .; rm -rf build; git add -A; git commit -a -m 'deploy'; git push; git checkout develop; rm -rf static",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
