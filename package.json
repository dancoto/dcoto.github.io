{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://dcoto.github.io/",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "npm run adjust; git checkout master; find build -type f -exec mv {} . \\;;  git add -A; git commit -a -m 'deploy'; git push; git checkout develop;",
    "nodeploy": "npm run build; npm run adjust; git checkout master; find build -type f -exec mv {} . \\;;",
    "adjust-files": "replace-in-file /\/?static\/[a-z]+\//g / build/**/*.html,build/**/*.js,build/**/*.css --isRegex;",
    "adjust-references": "replace-in-file /\/?static\/[a-z]+\//g '' build/**/*.json,build/**/*.map --isRegex;",
    "adjust":"npm run adjust-files; npm run adjust-references",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "replace-in-file": "^3.0.0-beta.2"
  }
}
